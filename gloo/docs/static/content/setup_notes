This guide assumes that you have installed Gloo into the `gloo-system` namespace, and `glooctl` is installed to your machine.
Let's also make sure that URL of the Gloo Gateway is set in your terminal:

```shell
export GATEWAY_URL=$(glooctl proxy url)
```
